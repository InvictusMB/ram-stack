const Injected = {
<% ruleMatches.map(({registration}) => { %>
  <%= registration %>: '<%= registration %>',
<% }) %>
  Shell: 'Shell',
  ContainerContext: 'ContainerContext',
  routerRoot: 'routerRoot',
};

window.Injected = Injected;
